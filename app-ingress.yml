apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: snowflake-app-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: snowflake.dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: snowflake-app-service
                port:
                  number: 80

